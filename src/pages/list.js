import React, { Component } from 'react';
import Header from '../components/Header';

class List extends Component {
    render() {
        return (
                <Header />
            );
        }
    }

export default List;